# Leave API/WS empty for Docker and same-origin production builds (nginx proxies /api and /ws).
# For Koyeb deploy only, set at build time:
#   VITE_API_URL=https://your-backend.koyeb.app/api/
#   VITE_WS_URL=wss://your-backend.koyeb.app/ws/notifications/
